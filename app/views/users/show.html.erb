<div class='account-page' id='account-page'>
  <div class='account-page__inner clearfix'>
    <div class='account-page__inner--left account-page__header'>
      <h2><%= @user.name %>さんのAccount</h2>
      <%= link_to "トップページ", root_path, class: 'btn'%>
      <%= link_to "チャットルームを作る", :back, class: 'btn'%>
      <br>
      <%= link_to "アカウントの編集", edit_user_path(current_user), class: 'btn' %>
      <%= link_to "ログアウト",destroy_user_session_path, method: :delete, class: 'btn'%>
    </div>

    <table class="table">
      <tr>
        <th class="table__col1">名前</th>
        <td class="table__col2"><%= @user.name %></td>
      </tr>
      <tr>
        <th class="table__col1">所属</th>
        <td class="table__col2"><%= @user.occupation %></td>
      </tr>
      <tr>
        <th class="table__col1">役職</th>
        <td class="table__col2"><%= @user.position %></td>
      </tr>
    </table>

    <div class=my-messages>
      ＜チャットルームでのコメント一覧＞
      <div class="messages">
        <%= render partial: 'messages/message', collection: @user.messages %>

      </div>

    </div>

    <div class=my-messages>
      ＜掲示板でのコメント一覧＞
      <div class="messages">
        <%= render "share_messages/messages" %>
      </div>

    </div>



  </div>
</div>